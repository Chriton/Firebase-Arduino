<template>
  <div>
    <p>The Sign up page</p>
  </div>
</template>

<script>
    export default {
        name: "signup",
        data () {
          return {
            // email: '',
            // emailRules: [
            //   v => !!v || 'E-mail is required',
            //   v => /^\w+([.-]?\w+)*@\w+([.-]?\w+)*(\.\w{2,3})+$/.test(v) || 'E-mail must be valid'
            // ],
            // password: '',
            // passwordRules: [
            //   v => !!v || 'Password is required',
            //   v => (v && v.length >= 6) || 'Password must be at least than 6 characters'
            // ]
          }
        }
    }
</script>

<style scoped>

</style>
